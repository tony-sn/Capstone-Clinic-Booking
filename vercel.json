{
  "version": 2,
  "buildCommand": "pnpm --filter fe build",
  "outputDirectory": "fe/.next",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "@DOTNET_API_URL/api/$1"
    }
  ]
}
